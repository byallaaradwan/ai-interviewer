{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker backend — scaffold only; populated in Phase 2 (auth + D1)",
  "scripts": {
    "dev": "echo 'Worker scaffold — populated in Phase 2' && exit 0"
  }
}
