{
  "name": "ai-interviewer",
  "version": "0.0.0",
  "private": true,
  "description": "AI-powered user interviewer agent — monorepo",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview"
  },
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20"
  }
}
